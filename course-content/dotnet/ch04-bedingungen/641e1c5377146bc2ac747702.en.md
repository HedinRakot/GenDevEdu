`Console.ReadLine` - this command is used when you want to ask something from the user. They enter data, and it stays in the program.

If you want to remember and use what your client entered, you need to use a variable.

Example:

```csharp
var name = Console.ReadLine();
```

In this example, we assign the value that the user enters from the keyboard to the variable `name`.
