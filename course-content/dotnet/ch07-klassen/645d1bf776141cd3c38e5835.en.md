**1.** Add a function to the previously created class `Tourist` that returns text containing information about the tourist's name and the travel destination.

**2.** Add a function to the previously created class `Outerwear` that asks the user for the clothing category and prints the full information about the clothing item, including the category, to the screen. Use the `ShowText` function.

**3.** Add a function to the previously created class `Student` that asks the user whether they like the chosen subject. If the user answers "yes", print "The lesson will start in an hour" to the screen. Otherwise, ask which subject they like better. Save the answer inside the class `Student`.
